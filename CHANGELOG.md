# Changelog
All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](http://keepachangelog.com/en/1.0.0/)
and this project adheres to [Semantic Versioning](http://semver.org/spec/v2.0.0.html).

## 1.0.12
- `읽기 기본 표시`를 꺼도 검색 응답 처리 시 읽기 대분류가 다시 기본 선택되던 문제를 수정했습니다.

## 1.0.11
- `읽기 포함` 체크박스를 `읽기 기본 표시` 설정으로 바꿨습니다.
- `읽기 기본 표시`는 현재 팝업을 즉시 필터링하지 않고, 다음 팝업이 열릴 때 읽기 대분류의 초기 선택 상태만 결정합니다.
- 읽기 대분류와 읽기 상세 필터는 숨기지 않고 기존 필터 칩으로 직접 켜고 끌 수 있게 유지했습니다.

## 1.0.10
- 결과 팝업의 유형 필터 줄에 `읽기 포함` 체크박스를 추가했습니다.
- `읽기 포함` 설정은 사용자별로 저장되며, 최초 기본값은 켜짐입니다.
- 체크박스를 끄면 읽기 대분류, 읽기 상세 필터, 읽기 결과가 함께 숨겨집니다.

## 1.0.9
- 대분류/소분류 필터의 한글 표시 문자열 인코딩을 정상화했습니다.
- 쓰기/읽기 필터에 Rider 기본 read/write access 아이콘을 적용했습니다.
- 전달·호출 대분류는 전달 아이콘, 직접 호출 소분류는 메서드 아이콘을 사용합니다.

## 1.0.8
- 대분류를 `쓰기`, `전달·호출`, `읽기`로 단순화했습니다.
- 읽기 결과도 기본 표시되도록 바꾸고 정렬 순서를 `쓰기`, `전달·호출`, `읽기`로 유지합니다.

## 1.0.7
- `ref`, `out`, `in`으로 컬렉션 자체를 인자로 넘기는 호출을 `전달·호출 > 참조 인자`로 감지합니다.

## 1.0.6
- `var alias = collection`, object initializer, tuple 안의 컬렉션 참조를 `컬렉션 레퍼런스`로 감지합니다.
- null 비교, 컬렉션 값 비교, `ReferenceEquals`를 `읽기 > 조건 비교`로 감지합니다.
- LINQ 읽기 API와 query expression 감지를 보강했습니다.
- 직접 원소 접근 뒤 이어지는 메서드 호출을 `직접 호출` 소분류로 분리했습니다.

## 1.0.5
- 컬렉션 자체를 인자, 반환값, 대입 RHS로 넘기는 사용 위치를 `컬렉션 레퍼런스`로 감지합니다.
- 레퍼런스 전달 상세 필터를 `원소 레퍼런스`와 `컬렉션 레퍼런스`로 분리했습니다.

## 1.0.4
- 선언과 동시에 컬렉션 값을 할당하는 `var map = ...`, `Dictionary<TKey, TValue> map = ...` 패턴을 `컬렉션 초기화`로 감지합니다.
- `컬렉션 대입` 대분류 아래에서 `컬렉션 초기화`와 기존 대입을 상세 동작으로 구분합니다.

## 1.0.3
- 상세 동작 필터에서 `내용물 수정`과 삭제 계열이 같은 배경 계열로 보이는 색상 문제를 조정했습니다.
- `내용물 수정`은 보라 계열, `전체 삭제` 등은 명확한 배경 계열로 분리했습니다.

## 1.0.2
- 읽기 계열 사용 위치 감지와 `읽기` 대분류/상세 필터를 추가했습니다.
- 읽기 계열은 기존 수정·전달 결과를 방해하지 않도록 별도 필터로 분리했습니다.

## 1.0.1
- 대분류 필터를 하위 필터와 구분되는 탭 형태로 조정했습니다.
- 접힌 대분류에 속한 상세 필터를 숨기도록 조정했습니다.

## 1.0.0
- C# BCL 컬렉션 전용 사용 위치를 검색하는 Rider 액션을 추가했습니다.
- Rider 2024.3 계열, build 243 이상을 지원합니다.
- `List`, `IList`, `ICollection`, `Collection`, `Dictionary`, `IDictionary`, `HashSet`, `ISet`, `Queue`, `Stack`, 배열을 지원합니다.
- 원소 추가, 원소 삭제, 전체 삭제, 순서 변경, 집합 연산, 큐/스택 연산, 인덱서 교체 같은 컬렉션 구조 변경을 감지합니다.
- 컬렉션 자체 대입, 컬렉션 원소의 내용물 수정, 원소 alias 생성, 단순 원소 escape를 감지합니다.
- 결과 수, 대분류 필터, 상세 동작 필터, 접기/펼치기, 코드 이동, Rider 스타일 코드 프리뷰를 제공하는 전용 팝업을 추가했습니다.
- 전용 팝업에서 Rider 기본 Find Usages로 넘어가는 `전체 사용 위치 찾기` 버튼을 추가했습니다.
- Rider/ReSharper 버전에 따라 generic read lock API를 찾지 못하던 백엔드 실행 호환성 문제를 수정했습니다.
- 기본 단축키 `Ctrl+F12`와 에디터 우클릭 메뉴 항목을 추가했습니다.
